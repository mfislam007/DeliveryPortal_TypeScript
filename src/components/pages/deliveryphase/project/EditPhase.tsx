import React from "react";

interface Props {
  start: Date;
  end: Date;
}
const EditPhase: React.FC<Props> = (props): JSX.Element => {
  return <div>Edit phases</div>;
};

export default EditPhase;
